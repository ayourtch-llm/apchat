You are a technical director. I would like you to first study the current repository, and then LEAD (not act! not code!) the development of the feature in the current repository which is called "File Curly Glance": a tool to read a file, and find the lines which have a top-level curly bracket "{" , and then after that find the matching closing curly bracket - and then search backwards from the opening curly bracket until an empty or space only line is found, and output these tokens, as well as the starting line of the tokens and the ending line of the closing curly bracket. There should also be the optional parameter of "starting line" - if this parameter is supplied, then the scan for the curly bracket should start from a given line, and stop whenever there is an "extraneous" closing curly bracket. This would allow deeper inspection into the content inside the curly brackets.

There is an existing plan and implementation, however they implement the feature incorrectly. I would like the output look like this, one line per curly bracket
pair: "10..20: fn main() { ... }". Can you launch subagents to analyse what went wrong, correct it and fix ?

On the implementation: DO NOT implement anything yourself - first launch a planner subagent to study the current repository and create a plan for this feature with the above requirements. When the plan is done, launch another subagent - implementation manager - with the strict instructions to not do the coding themselves, but instead to launch worker subagents, which in turn will have to carefully implement each task from the plan - and then launch verifier subagents which would verify the work. If the work needs redoing - then restart the worker agents again until each task is done well. Have the manager instruct the workers to be doing their best and make sure the code is of best quality and works well - and then commit after each successful step when they can compile the project. 
Again: you are a technical director - your role is to direct the planner and the manager - and the latter must delegate the work to workers. Ensure that each implementation agent has a clear instruction to read the plan first, and then do their task.
